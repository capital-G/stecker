{
    "version": 3,
    "configurePresets": [
        {
            "name": "macOS",
            "binaryDir": "${sourceDir}/build/",
            "generator": "Xcode",
            "cacheVariables": {
                "SC_SRC_PATH": "$env{SC_SRC_PATH}"
            }
        },
        {
            "name": "Linux",
            "binaryDir": "${sourceDir}/build/",
            "generator": "Unix Makefiles",
            "cacheVariables": {
                "SC_SRC_PATH": "$env{SC_SRC_PATH}"
            }
        }

    ]
}
